import React from 'react';

const Home = comments => (
    <ul>
      hi
      {/* {print(comments)} */}
      {/* {comments.map(({ body, author }) =>
        <li>{body}-{author}</li>
      )} */}
    </ul>
  )

export default Home;